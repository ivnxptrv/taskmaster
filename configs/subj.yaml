programs:
  nginx:
    cmd: "nginx -p . -c ./apps/nginx/nginx.conf -e /dev/stderr"
    numprocs: 1
    umask: 022
    workingdir: .
    autostart: true
    autorestart: unexpected
    exitcodes:
      - 0
      - 2
    startretries: 3
    starttime: 5
    stopsignal: TERM
    stoptime: 10
    stdout: /tmp/nginx.stdout
    stderr: /tmp/nginx.stderr
  answer:
    cmd: "./apps/sigusr1me/run"
    numprocs: 5
    umask: 077
    workingdir: .
    autostart: true
    autorestart: never
    exitcodes: [0]
    startretries: 2
    starttime: 1
    stopsignal: USR2
    stoptime: 10
    stdout: /tmp/answer.stdout
    stderr: /tmp/answer.stderr
    env:
      STARTED_BY: taskmaster
      ANSWER: 42
